using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ToDo : MonoBehaviour
{
    
    /* each weapon needs its own class
     * this class will have cooldown, damage, knockback, maybe hitbox
     */

    /* get second attack working, this will be the jump off pole attack
     * this will fling the character upwards and deal damage when sticking into the ground
     * or maybe the damage will come from the character moving through the air
     */

    /* working on the animator right now. I want the PoleStyle and the NoWeaponStyle to be the objects that deal with the animations
     * 
     */

    /* jump will also need to be changed so that when you jump, you will fall when you release
     * the button
     */
}
